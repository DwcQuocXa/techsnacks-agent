```json
{
  "topic": "ReactJS useEffect best practices and modern alternatives",
  "article": "# [#TechSnack 06] | useEffect: Dùng sao cho \"chuẩn\" trong kỷ nguyên React 19?\n\nChào ae, sau các bài về AI và Tooling, hôm nay mình quay lại với một chủ đề \"cũ mà mới\" trong React: **useEffect**. \n\nDù đã ra mắt từ lâu, `useEffect` vẫn là một trong những Hook gây tranh cãi và dễ bị lạm dụng nhất. Nhiều ae vẫn coi nó là giải pháp cho mọi vấn đề từ fetch data, sync state cho đến handle logic sự kiện. Tuy nhiên, với sự tiến hóa của React 19, tư duy dùng effect của chúng ta cần phải thay đổi.\n\n## Tại sao \"You Might Not Need an Effect\"?\n\nReact Team đã nhấn mạnh: `useEffect` là để **synchronization** (đồng bộ hóa) với các hệ thống bên ngoài (như Browser API, WebSockets), không phải để quản lý logic nội bộ của component.\n\nMột sai lầm kinh điển là dùng `useEffect` để tính toán state dựa trên props (**derived state**). Thay vì tạo thêm một state mới và cập nhật nó trong effect, ae nên tính toán trực tiếp ngay trong quá trình render. Việc này giúp code sạch hơn và tránh được một lượt re-render không đáng có.\n\n## Những Anti-patterns cần tránh\n\n- **Race conditions**: Khi fetch data trong effect, nếu không có cleanup function để hủy request cũ (dùng `AbortController` hoặc ignore flag), kết quả trả về chậm có thể ghi đè lên data mới nhất, gây bug hiển thị sai lệch.\n- **Event-specific logic**: Logic xảy ra sau khi user click nút thì nên nằm ở event handler. Đừng đẩy vào `useEffect` chỉ để \"theo dõi\" một state nào đó thay đổi, điều này làm flow code cực kỳ khó debug.\n- **Infinite loops**: Cập nhật chính state nằm trong dependency array mà không có điều kiện dừng sẽ khiến component re-render vô tận.\n\n## Modern Alternatives trong React 19\n\nVới sự xuất hiện của React 19 và **React Compiler**, workflow của chúng ta đang thay đổi mạnh mẽ:\n\n- **use() Hook**: Thay thế phần lớn pattern fetch data truyền thống. Kết hợp với `Suspense`, code trông declarative hơn hẳn và không cần quản lý `isLoading` thủ công.\n- **useSyncExternalStore**: Nếu ae cần subscribe vào các external store, đây là lựa chọn an toàn hơn để tránh hiện tượng **tearing** trong concurrent rendering.\n- **React Compiler**: Tự động hóa việc memoization, giúp ae bớt phải lo lắng về việc giữ stable cho dependency array.\n\nTóm lại, hãy coi `useEffect` là lựa chọn cuối cùng khi không còn cách nào khác để tương tác với thế giới bên ngoài component.\n\n## Giờ đến lượt ae:\n\n- Ae đã bắt đầu \"clear\" bớt `useEffect` trong project của mình chưa?\n- Có pattern nào ae thấy cực kỳ hiệu quả để thay thế effect không?\n\nChia sẻ kinh nghiệm bên dưới để ae cùng bàn luận nhé!\n\n**Happy coding!! 😁**",
  "word_count": 395,
  "validation": {
    "title_format": true,
    "length": true
  },
  "errors": []
}
```